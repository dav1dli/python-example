apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "python-example.fullname" . }}
  labels:
    {{- include "python-example.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "python-example.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "python-example.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.imagePullSecret.use }}
      imagePullSecrets:
        - name: {{ include "python-example.fullname" . }}-pull-secret
      {{- end }}
      serviceAccountName: {{ include "python-example.serviceAccountName" . }}-sa
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
        - name: {{ .Chart.Name }}-front
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.frontImage.repository }}:{{ .Values.frontImage.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.frontImage.pullPolicy }}
          ports:
            - name: front-http
              containerPort: {{ .Values.frontService.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: front-http
          readinessProbe:
            httpGet:
              path: /
              port: front-http
          resources:
            {{- toYaml .Values.frontResources | nindent 12 }}
          volumeMounts:
            - name: nginx-data
              mountPath: /opt/app-root/src
            - name: nginx-api
              mountPath: /opt/app-root/etc/nginx.default.d
      volumes:
        - name: nginx-data
          configMap:
            name: {{ include "python-example.fullname" . }}-configmap
        - name: nginx-api
          configMap:
            name: {{ include "python-example.fullname" . }}-api
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
